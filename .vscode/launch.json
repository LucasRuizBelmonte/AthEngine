{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug AthEngine",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/athengine.exe",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "preLaunchTask": "Build"
        }
    ]
}